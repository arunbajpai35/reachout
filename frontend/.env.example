# Local dev: leave empty. Vite's proxy in vite.config.ts forwards /api to localhost.
# Production (e.g. Vercel): set this to the deployed backend URL (no trailing /api/v1).
# Example: VITE_API_BASE_URL=https://reachout-api.onrender.com
VITE_API_BASE_URL=
